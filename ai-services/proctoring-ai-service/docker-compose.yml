services:
  proctoring-ai:
    build: .
    ports:
      - "8800:8800"
    environment:
      PROCTORING_MODEL: /models/yolo26s.pt
    volumes:
      - ../models:/models:ro
      - proctoring-models:/root/.cache/ultralytics
    restart: unless-stopped

volumes:
  proctoring-models:
